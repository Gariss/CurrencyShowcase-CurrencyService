<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>CurrencyService.$(MSBuildProjectName.Replace(" ", "_"))</RootNamespace>
    <AssemblyName>CurrencyService.$(MSBuildProjectName)</AssemblyName>
  </PropertyGroup>

</Project>
